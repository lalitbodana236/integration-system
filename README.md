# integration-system

GitHub Pages demo lives in `docs/`.

To publish it:
1. Open the repo settings on GitHub.
2. Set **Pages** source to the `docs/` folder on the default branch.
3. Wait for the Pages build to finish, then open the published URL.
